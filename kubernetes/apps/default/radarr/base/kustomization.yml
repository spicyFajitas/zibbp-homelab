apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: radarr

commonLabels:
  app: radarr

resources:
  - deployment.yml
  - service.yml
  - ingress.yml
  - pvc.yml

configMapGenerator:
  - name: radarr-config
    files:
      - radarr-config.yml

images:
  - name: radarr
    newName: lscr.io/linuxserver/radarr
    newTag: 4.2.4.6635-ls158
